<nav class="bg-white shadow-md p-4 flex justify-between items-center">
  <div class="text-2xl font-bold text-blue-600">CustomText</div>
  <div class="space-x-4">
    <% if (user) { %>
      <span class="text-gray-700">Hello, <%= user.name %></span>
      <a href="/user/logout" class="text-gray-700 hover:text-blue-500">Logout</a>
    <% } else { %>
      <a href="/user/signin" class="text-gray-700 hover:text-blue-500">Sign In</a>
      <a href="/user/signup" class="text-gray-700 hover:text-blue-500">Sign Up</a>
    <% } %>
  </div>
</nav>
