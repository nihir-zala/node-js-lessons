<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BlogSite</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>

        <% if(user){ %>
        <li class="nav-item">
          <a class="nav-link" href="/blog/my-blogs">My Blogs</a>
        </li>
        <% } %>
      </ul>

      <!-- Right side nav -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if(user){ %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
            <img src="<%= user.profileImageURL ? '/' + user.profileImageURL : '/images/default-avatar.jpg' %>" class="rounded-circle me-2" style="width:35px; height:35px;">
            <span><%= user.fullName %></span>
          </a>
          <ul class="dropdown-menu dropdown-menu-dark" style="margin-left:-8px">
            <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
          </ul>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/user/signin">Sign In</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/user/signup">Sign Up</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>